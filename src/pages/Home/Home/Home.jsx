import React, { useContext } from 'react';
import Banner from '../Banner/Banner';
import Flags from '../Flags/Flags';
const Home = () => {

    return (
        <div>
            <Banner/>
            <Flags/>
        </div>
    );
};

export default Home;